#!/usr/bin/env node

const lnk = require('lnk')
const fs = require('fs')

// if webpack deps not at the top node_modules
// then link them from this sub node_modules

function checkModule (name) {}

function linkModule (name) {}
